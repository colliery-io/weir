{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "weir-server.serviceAccountName" . }}
  labels:
    {{- include "weir-server.labels" . | nindent 4 }}
{{- end }}
