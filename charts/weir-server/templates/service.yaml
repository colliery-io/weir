apiVersion: v1
kind: Service
metadata:
  name: {{ include "weir-server.fullname" . }}
  labels:
    {{- include "weir-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "weir-server.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
