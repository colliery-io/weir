# weir `snowflake-dest` connector

Scaffolded by `angreal connectors new`. See docs/guides/connector-authoring.md.

- **Roles**: Source (edit `spec`).
- **Auth**: resolved host-side ([[WEIR-A-0033]]); no secrets in the guest.
