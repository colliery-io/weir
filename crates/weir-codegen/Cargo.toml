[package]
name = "weir-codegen"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Lowers a declarative manifest into a fidius Connector crate — both a native dylib and a self-contained WASM guest (WEIR-A-0014 §2)."

[[bin]]
name = "weir-codegen"
path = "src/main.rs"

[dependencies]
weir-manifest = { path = "../weir-manifest" }
